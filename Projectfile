# alpaca2pg (see github.com/python-medikit)

from medikit import require

python = require('python')
git = require('git')
make = require('make')

python.setup(
    name='alpaca2pg',
    description='Alpaca to PostgreSQL ETL',
    license='Apache License, Version 2.0',
    url='',
    download_url='',
    author='',
    author_email='',
)

python.add_requirements(
    # Insert your requirements here. We suggest you use minor version requirements here, and freeze an exact version in
    # requirements*.txt (which, when does not exist, can be generated by medikit).
    'bonobo', )

# vim: ft=python:
